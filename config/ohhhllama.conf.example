# ohhhllama Configuration
# Copy this file to /opt/ohhhllama/ohhhllama.conf and customize as needed
# After changes, restart the service: sudo systemctl restart ollama-proxy

# Ollama backend URL
# The actual Ollama instance running in Docker on port 11435
OLLAMA_BACKEND=http://127.0.0.1:11435

# Proxy listen port
# This is the port clients connect to (standard Ollama port)
LISTEN_PORT=11434

# SQLite database path
# Stores the download queue and rate limit data
DB_PATH=/var/lib/ohhhllama/queue.db

# Max model requests per IP per day
# Set to 0 to disable rate limiting
RATE_LIMIT=5

# Disk space monitoring
# Path to monitor for disk space (should be where Ollama stores models)
DISK_PATH=/data/ollama

# Disk usage threshold (percent)
# New pull requests will be rejected when usage exceeds this threshold
DISK_THRESHOLD=90

# Auto-cleanup
# Remove completed/failed queue entries older than this many days
CLEANUP_DAYS=30

# Queue processing schedule
# The schedule is controlled by the systemd timer, not this config file.
# To change when queue processing runs, edit the timer:
#   sudo nano /etc/systemd/system/ollama-queue.timer
# Then reload: sudo systemctl daemon-reload && sudo systemctl restart ollama-queue.timer
# Default: 3 AM daily (OnCalendar=*-*-* 03:00:00)
