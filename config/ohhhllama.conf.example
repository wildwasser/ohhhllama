# ohhhllama Configuration
# Copy to /opt/ohhhllama/ohhhllama.conf and customize

# =============================================================================
# Proxy Settings
# =============================================================================

# Ollama backend URL (the actual Ollama instance)
OLLAMA_BACKEND=http://127.0.0.1:11435

# Port for the proxy to listen on (clients connect here)
LISTEN_PORT=11434

# =============================================================================
# Queue Settings
# =============================================================================

# SQLite database path for the download queue
DB_PATH=/var/lib/ohhhllama/queue.db

# Rate limit: maximum model requests per IP per day
RATE_LIMIT=5

# Days to keep completed/failed entries before cleanup
CLEANUP_DAYS=30

# =============================================================================
# Disk Monitoring
# =============================================================================

# Path to monitor for disk space (should be where models are stored)
DISK_PATH=/data/ollama

# Disk usage threshold (percent) - downloads blocked above this
DISK_THRESHOLD=90

# =============================================================================
# HuggingFace Settings
# =============================================================================

# Cache directory for HuggingFace downloads
HF_CACHE_DIR=/data/huggingface

# Default quantization for HuggingFace models
# Options: Q8_0, Q6_K, Q5_K_M, Q5_K_S, Q4_K_M, Q4_K_S, Q4_0, Q3_K_M, Q3_K_S, Q2_K
DEFAULT_QUANT=Q4_K_M

# HuggingFace token for gated models (optional)
# Get from: https://huggingface.co/settings/tokens
# HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxx

# =============================================================================
# Docker Settings
# =============================================================================

# Ollama Docker container name
OLLAMA_CONTAINER=ollama
